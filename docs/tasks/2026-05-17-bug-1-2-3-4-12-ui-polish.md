# 2026-05-17 — Bug 1·2·3·4·12 UI 폴리시

## 목표
사용자 발견 UI 폴리시 5건을 일괄 수정.
- Bug 1: recommended 도장 "RECOMMENDED" → "추천" 한글화
- Bug 2: 카트(인벤토리)에서 메뉴 이미지·코드·서브타이틀이 사라지는 문제
- Bug 3: 헤더/로그인 마스코트 로고가 검은 단색으로 표시되는 문제
- Bug 4: 계좌번호 복사 시 예금주("박동빈")까지 같이 복사되어 송금 양식이 깨짐
- Bug 12: 5/20 오픈 시간을 16:30 → 15:00 으로 앞당기고 안내 문구 통일

## 만든 것
신규 파일 없음. 기존 파일 수정만.

## 한 일

### Bug 2 — cart store image/code/sub 스냅샷 (TDD)
- `src/store/cart.js`
  - `addItem` 신규 푸시 시 `image`, `code`, `sub` 함께 저장.
  - 헤더 주석에 항목 모양·결정 근거 동기화.
- `src/store/__tests__/cart.store.test.js`
  - 신규 케이스 "★ Bug 2 — addItem이 image/code/sub 메타데이터 보존" 추가.
  - 회귀: `cart-selector-rerender.test.jsx` 영향 없음.

### Bug 1 — StampBadge recommended 한글 라벨 (TDD)
- `src/components/molecules/StampBadge.jsx`
  - `VARIANT_CONFIG.recommended.label`: `'RECOMMENDED'` → `'추천'`.
  - aria-label 자동으로 "추천 도장"이 됨.
  - PAID/DONE/SOLD OUT/CANCELED 는 영문 도장 미학 유지.
- `src/components/molecules/__tests__/StampBadge.test.jsx`
  - it.each 라벨 검증, fallback 검증, customLabel 검증 모두 '추천' 기대값으로 update.
- `src/components/organisms/__tests__/MenuCard.test.jsx`
  - recommended 도장 표시 케이스 및 sold-out 우선 케이스의 기대 텍스트를 '추천'으로 update.

### Bug 4 — 계좌 복사 문구 예금주 제외 (TDD)
- `src/pages/customer/CompletePage.jsx`
  - `ACCOUNT_TEXT`: `'국민은행 233001-04-403536 박동빈'` → `'국민은행 233001-04-403536'`.
  - 화면 안내 `.acc-bank` (`국민은행 · 예금주 박동빈`)는 그대로 노출 — 사용자에게 예금주 정보 보여줄 가치 유지, 복사 문자열만 제한.
  - 페이지 ≤120 줄 회귀(`§3.5 1조`) 충족을 위해 주석을 1줄로 압축.
- `src/pages/customer/__tests__/CompletePage.test.jsx`
  - 신규 케이스 "★ Bug 4 — 계좌 복사 문구는 ... (예금주 미포함)" 추가.
  - 기존 Clipboard 케이스는 그대로 통과.

### Bug 12 — 5/20 오픈 시간 15:00 통일 (TDD)
- `src/components/organisms/ClosedScreen.jsx`
  - `REASON_CONFIG['before-open'].body`: "16:30부터 시작합니다" → "오후 3시에 오픈할 예정입니다".
  - `OPERATING_SCHEDULE[0].hours`: `'16:30 ~ 21:00'` → `'15:00 ~ 21:00'`.
  - 5/21은 그대로 (`11:00 ~ 21:00`).
- `src/store/businessState.js`
  - `SCHEDULE['2026-05-20'].start`: `'16:30'` → `'15:00'`.
  - 주석 동기화.
- `server/db/init.sql`
  - `system_settings('business_open_time')`: `'16:30'` → `'15:00'`.
  - 주석에 변경 근거 명시.
- `src/components/organisms/__tests__/ClosedScreen.test.jsx`
  - 운영 일정 케이스의 5/20 hours 기대값을 `'15:00 ~ 21:00'`으로 update.
  - 신규 케이스 2건:
    - "★ Bug 12 — 5/20 오픈 시간은 15:00 ~ 21:00"
    - "★ Bug 12 — before-open 문구는 '오후 3시에 오픈할 예정' 포함"
- `src/store/__tests__/businessState.store.test.js`
  - "시작 전 false" 케이스의 시각을 `15:00 → 14:59`로 update (15:00은 이제 영업 중).
  - 17:00 영업 중 케이스는 그대로.
- 회귀: `server/domain/__tests__/business-state.test.js` (도메인)은 hh:mm 자체 값을 검증하지 않으므로 영향 없음 — 그대로 통과.

### Bug 3 — 헤더 로고 검은 단색 (CSS-only, 수동 QA)
- `src/styles/components.css`
  - `.app-header .brand-mark` (28px):
    - `background-size: cover` → `contain`
    - `background-color: #1a1d12` → `transparent`
    - `background-repeat: no-repeat` 명시
  - `.login-mark .brand-mark` (48px) — 동일 처리.
  - 자동 테스트 어려움 → 수동 QA 항목 (헤더에 마스코트 png 정상 노출 확인).

## 테스트 결과
- `npx vitest run` 풀런: **987 passed / 987** (94 files).
- 도메인 회귀(pricing/coupon/order-state/business-state/settlement) 0 실패.
- 영향받아 update된 기존 테스트:
  - `src/components/molecules/__tests__/StampBadge.test.jsx` (Bug 1)
  - `src/components/organisms/__tests__/MenuCard.test.jsx` (Bug 1, 텍스트 의존)
  - `src/components/organisms/__tests__/ClosedScreen.test.jsx` (Bug 12)
  - `src/store/__tests__/businessState.store.test.js` (Bug 12)

## 수동 QA 항목
- [ ] **Bug 3** — 사용자/관리자 헤더에 마스코트 png 정상 노출 확인 (`docker compose up` 후 / 또는 `npm run dev`).
- [ ] **Bug 3** — `/admin/login` 화면 48px 마스코트도 동일 정상 노출.
- [ ] **Bug 2** — 메뉴 추가 → /cart 진입 → 메뉴 이미지·CODE 태그·서브타이틀 정상 표시.
- [ ] **Bug 1** — 메뉴 카드 추천 도장이 "추천" 한글로 표시되는지 확인.
- [ ] **Bug 4** — /complete 화면 "계좌번호 복사" 클릭 → 클립보드에 "국민은행 233001-04-403536"만 들어가는지 확인 (예금주 박동빈 미포함).
- [ ] **Bug 12** — 5/20 오픈 전 (예: 14:30) `/`에 접속하면 "오후 3시에 오픈할 예정" 문구와 "15:00 ~ 21:00" 일정이 노출되는지 확인.

## 다음에 할 것
- 남은 task #7 (Bug 11 — 관리자 내역/쿠폰탭 정책 반영).
- task #8 (전체 lint·build 실행).
- task #9 (최종 QA 보고).

## 제약 검증
- `.env`, 비밀파일 수정: **X**.
- 도메인 테스트(pricing/coupon/order-state) 변경: **X**.
- 계좌번호 자체(`233001-04-403536`) 변경: **X** (복사 문자열만 변경).
- design-bundle 폴더 변경: **X**.
- 모든 주석·문구는 한국어.
