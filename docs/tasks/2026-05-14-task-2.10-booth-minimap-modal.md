# Task 2.10 — BoothMinimapModal organism

## 목표

G12 부스 약도 — PUBG 미니맵 풀스크린 모달 + 본인 테이블 펄스 강조 + 하단 큰 닫기 버튼 (결정 e) 구현.

- 4가지 닫기 방식 (하단 버튼 · 상단 X · backdrop · Esc) — `onClose(reason)`
- 본인 테이블 펄스 매번 재생 (결정 h, sessionStorage 없음)
- `prefers-reduced-motion` 시 펄스 → 정적 box-shadow
- `mapImage` 미수령 시 CSS 그리드 fallback (자산 D-1 수령 — 임시 대응)
- 모달 a11y: `role="dialog"` + `aria-modal="true"` + `aria-labelledby` + 포커스 트랩/복귀 + body scroll lock

UX §8.7 / IMPLEMENTATION_PLAN §2.10 / G12.

## 만든 것

- `src/components/organisms/BoothMinimapModal.jsx` — 신규 organism (≈ 130 줄)
- `src/components/organisms/__tests__/BoothMinimapModal.test.jsx` — 15 케이스
- `src/styles/components.css` — `@keyframes booth-table-pulse` + `.booth-table-pulse` + reduced-motion 정적 box-shadow

## 한 일

### 1. RED — 테스트 먼저 (15 케이스)

명세 14 케이스를 충족하기 위해 다음을 작성. 추가로 scroll unlock 회귀 1 케이스 더 (총 15):

1. `open=false` 시 `null` 렌더
2. `role=dialog` + `aria-modal=true` + `aria-labelledby="minimap-title"`
3. 하단 큰 닫기 버튼 클릭 → `onClose('bottom-close')` (결정 e)
4. 상단 X 버튼 클릭 → `onClose('top-x')`
5. backdrop 클릭 → `onClose('backdrop')` (`e.target === e.currentTarget` 가드)
6. Esc 키 → `onClose('escape')` (document 레벨)
7. `mapImage` 미수령 시 fallback grid (4×4 = 16 셀)
8. `mapImage` 있을 시 `<img alt="부스 위치 약도">` + fallback X
9. 본인 테이블 셀 `.booth-table-pulse` 클래스 (결정 h)
10. 다른 테이블 셀은 pulse 없음
11. open 시 하단 닫기 버튼 자동 포커스
12. close 시 직전 활성 요소(trigger)로 포커스 복귀
13. body `overflow=hidden` (scroll lock)
14. close 시 overflow 복귀
15. axe-core 위반 0

vitest 결과: 컴포넌트 미존재 → `Failed to resolve import` (예상된 RED).

### 2. GREEN — 컴포넌트 구현

`BoothMinimapModal.jsx` (forwardRef, `useEffect` 단일):

- `open=true` 진입 시:
  - `previousFocusRef.current = document.activeElement`
  - `closeBtnRef.current?.focus()`
  - `document.addEventListener('keydown', onKeyDown)` — Esc 처리
  - `document.body.style.overflow = 'hidden'`
- cleanup (open=false 또는 unmount):
  - 리스너 제거 + overflow 복귀 + 이전 포커스 복귀
- `if (!open) return null` → 마운트는 하되 비렌더 (rerender open=false 패턴 지원).

API:

```jsx
<BoothMinimapModal
  open={true}
  myTableNo={5}
  mapImage="/map/booth-map.png"   // 미수령 시 undefined
  gridSize={{ cols: 4, rows: 4 }}
  onClose={(reason) => ...}
/>
```

### 3. REFACTOR — axe 위반 수정 (gridcell 부모 row 누락)

초기 GREEN 라운드에서 14/15 통과. 1 실패: axe `aria-required-parent` —
`role="gridcell"` 은 `role="row"` 부모 필수. 셀을 row 단위로 래핑하고
`className="contents"` 로 CSS Grid 레이아웃에는 영향 없도록 처리.

```jsx
{Array.from({ length: rows }, (_, rowIdx) => (
  <div key={`row-${rowIdx}`} role="row" className="contents">
    {Array.from({ length: cols }, (_, colIdx) => { ... })}
  </div>
))}
```

### 4. CSS keyframes

`src/styles/components.css` 끝에 추가:

```css
@keyframes booth-table-pulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(244, 210, 0, 0.7); }
  50%      { box-shadow: 0 0 0 12px rgba(244, 210, 0, 0); }
}
.booth-table-pulse {
  animation: booth-table-pulse 1.4s ease-in-out infinite;
}

@media (prefers-reduced-motion: reduce) {
  /* ... 다른 모션 클래스 ... */
  .booth-table-pulse {
    animation: none;
    box-shadow: 0 0 0 4px var(--color-accent);
  }
}
```

reduced-motion 시 펄스 대신 정적 box-shadow 유지 → 본인 위치 시각 강조는 유지.

## 테스트 결과

```
Test Files  33 passed (33)
     Tests  327 passed (327)    ← 312 + 15
  Duration  23.09s
```

`npm run build` — 1.58s, 회귀 없음. CSS bundle 18.09 kB (gzip 4.93 kB).

## 다음에 할 것

- Task 2.11 (남은 organism) → §2.x 명세 참조
- 자산 D-1 수령 후: `mapImage` 정확한 좌표 매핑. 현재 이미지 모드는 본인 테이블 마커를 정중앙 임시 배치 — 명세는 "펄스 작동 자체만 회귀" 라서 좌표 회귀 X.
- 페이지(예: `OrderDetailPage`) 통합 시 useState로 open 토글 + trigger 버튼 연결.
