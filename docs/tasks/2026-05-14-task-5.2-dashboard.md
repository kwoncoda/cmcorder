# Task 5.2 — A-2 DashboardPage (Kanban 6컬럼 + G13 영업 토글)

**작업일:** 2026-05-14
**커밋:** `7c96e28 feat(task-5.2): A-2 DashboardPage(Kanban 6컬럼 + G13 영업 토글) + 20 케이스`

## 목표

본부 운영자가 한 화면에서 (a) 영업 상태를 토글하고 (b) 6컬럼 Kanban으로 진행 주문을 추적할 수 있도록 한다.
이전 placeholder DashboardPage 를 실제 운영 UI로 대체.

핵심 결정:
- **페이지 ≤120줄** (§3.5 1조) — 표시 로직은 Organism(AdminCardColumn) 위임.
- **CLOSED 시** BusinessStateBadge + 큰 형광 옐로 StartBusinessCTA, Kanban 숨김.
- **OPEN 시** 헤더 + 6컬럼 Kanban + 5초 폴링 + 1분 tick.
- **5초 폴링** — `useApi.refetch` — OPEN 상태에서만 setInterval 등록 (CLOSED 시 X).
- **1분 tick** — `setTick(Date.now())` → AdminCardColumn `tick` prop → OrderCard `useMemo` 재계산.
- **401 → /admin/login** — render 중 navigate 금지(effect로 분리).
- **방어**: `Array.isArray(orders)` 가드 (App.test 의 mock apiFetch 가 객체 반환 케이스 회귀 차단).

## 만든 것

- `src/pages/admin/DashboardPage.jsx` (placeholder → 108줄 실 구현)
- `src/constants/admin-columns.js` — `ADMIN_COLUMNS` 6개 + `groupOrdersByStatus(orders)` 헬퍼
- `src/pages/admin/__tests__/DashboardPage.test.jsx` — 회귀 20 케이스

## 한 일

### 구현

1. `ADMIN_COLUMNS` 상수를 별도 파일로 분리 (status·title·color) — DashboardPage 줄 수 절약 + 재사용 가능.
2. `groupOrdersByStatus(orders)` — 6개 키 보장 + 비배열 방어.
3. `DashboardPage`:
   - `useBusinessStateStore` 셀렉터 3개 (status / setStatus / shouldBeOpen).
   - `useApi(({ signal }) => apiFetch(API.ADMIN_ORDERS, { signal }), [])` — 첫 진입 단발 + refetch.
   - 1분 tick `setInterval(60_000)` → `setTick(Date.now())`.
   - 5초 폴링 `setInterval(5_000)` → `refetch()` — `status==='OPEN'` 가드.
   - 401 redirect `useEffect(() => { if (error.status===401) navigate('/admin/login'); }, [error])`.
   - `handleStartBusiness()` — `apiFetch(API.ADMIN_BUSINESS_OPEN, { method: 'POST', body: {} })` → `setStatus('OPEN')` + `refetch()`. 실패 시 `setStartError(err.message)`.
   - CLOSED → `<BusinessStateBadge>` + `<StartBusinessCTA>` (Kanban 숨김).
   - OPEN → Loading / Error / Empty / Kanban 4분기.
   - `LoadingState minimumDelay={0}` — 첫 진입 즉시 표시 (페이지 fallback 시점이 명확).
4. `LoginPage.test.jsx` 등 다른 subagent(Task 5.1/5.3) 변경은 건드리지 않음 — 충돌 없는 파일만 작업.

### 테스트 20 케이스

| # | 케이스 |
|---|--------|
| 1 | CLOSED 시 BusinessStateBadge + StartBusinessCTA + Kanban 숨김 |
| 2 | "장사 시작" 클릭 시 API 호출 + status=OPEN 전이 |
| 3 | "장사 시작" API 실패 시 status 변동 X + cta-error 표시 |
| 4 | OPEN 시 BusinessStateBadge OPEN + Kanban 6 컬럼 모두 렌더 |
| 5 | Loading 분기 (LoadingState) |
| 6 | Error 분기 — 5xx ErrorState + 다시 시도 |
| 7 | Empty 분기 — "오늘 첫 주문 대기 중" |
| 8 | 401 시 /admin/login redirect |
| 9 | 주문 컬럼별 그룹화 (COOKING 2건, ORDERED 1건) |
| 10 | tick 패턴 — 1분 후 elapsed_minutes 5→6 재렌더 (act + advanceTimersByTime) |
| 11 | 5초 폴링 — OPEN 상태 5초 후 refetch |
| 12 | CLOSED 상태 폴링 X — 15초 진행해도 refetch 호출 X |
| 13 | 카드 클릭 시 /admin/orders/:id navigate |
| 14 | 키보드 Enter — OrderCard tagName === 'BUTTON' (Task 2.7 통합 회귀) |
| 15 | memo 회귀 — 동일 reference 재렌더 시 카드 그대로 존재 (Task 2.7 통합) |
| 16 | OPEN 시 헤더 "📋 본부 대시보드" 제목 |
| 17 | OPEN + Empty 시에도 헤더 + Badge 표시 |
| 18 | 페이지 ≤120줄 (§3.5 1조) — 108줄 확인 |
| 19 | a11y 위반 없음 (CLOSED) |
| 20 | a11y 위반 없음 (OPEN + Kanban) |

## 테스트 결과

- `npx vitest run src/pages/admin/__tests__/DashboardPage.test.jsx` → **20/20 통과**.
- `npx vitest run src/__tests__/App.test.jsx` → 19/19 통과 (DashboardPage `Array.isArray` 가드 덕분에 회귀 X).
- `npm run build` → 정상 (DashboardPage chunk 6.48 kB / gzip 2.91 kB).
- 전체 `npx vitest run` → 610/611 통과 (1 fail = Task 5.1 LoginPage a11y — 본 Task 와 무관, 동시 진행 subagent 책임).

## 다음에 할 것

- Task 5.4 (MenuAdminPage) / Task 5.5 (SettlementPage) 진행 시 본 페이지의 `useApi + 5초 폴링 + 1분 tick + 3분기` 패턴 재사용.
- Phase 6 백엔드 구현 후 실 API 통합 시:
  - 백엔드 `GET /admin/api/orders` 응답 스키마 (`order id/no/status/depositor_name/transferred_at`) 회귀 확인.
  - `POST /admin/api/business/open` 응답 — 본 페이지는 응답 body 무시 + setStatus('OPEN') 만 수행.
  - 실 운영 시 SSE 도입 시 5초 폴링은 fallback 으로 유지 (Phase 2 후보).
